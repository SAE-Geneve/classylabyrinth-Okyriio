
#include "character.h"

Character::Character(
    int x,
    int y,
    int healthpoints,
    int attack,
    int defence)
    :x(x),
    y(y),
    healthpoints(healthpoints),
    attack(attack),
    defence(defence) {
}
